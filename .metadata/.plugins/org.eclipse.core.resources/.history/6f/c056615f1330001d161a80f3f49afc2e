package objectcollection;

import java.util.HashSet;
import java.util.LinkedList;
import java.util.TreeMap;

import object.Student;
import object.Subject;

public class StudentSubject {
	private Student student;
	private Subject subject;
	private TreeMap<Subject, Float> treeScore= new TreeMap<Subject,Float>();
	private TreeMap<Student,TreeMap> mapStudent = new TreeMap<Student, TreeMap>();
	
	public StudentSubject() {
		this.student = new Student();
		this.treeScore = new TreeMap<Subject,Float>();
		this.mapStudent = new TreeMap<Student, TreeMap>();
	}

	public StudentSubject(Student student,TreeMap<Subject, Float> treeScore) {
		this.student = student;
		this.mapStudent.put(student, treeScore);
		
	}

	public Student getStudent() {
		return student;
	}

	public void setStudent(Student student) {
		this.student = student;
	}

	public HashSet<Subject> getListSubject() {
		return listSubject;
	}

	public void setListSubject(HashSet<Subject> listSubject) {
		this.listSubject = listSubject;
	}

	public TreeMap<Student, HashSet<Subject>> getMapStudent() {
		return mapStudent;
	}

	public void setMapStudent(TreeMap<Student, HashSet<Subject>> mapStudent) {
		this.mapStudent = mapStudent;
	}

	@Override
	public String toString() {
		String resuft = "";
		for (Subject subject: this.listSubject) {
			resuft += this.student.toString() + subject+", ";
		}
		return resuft;
	}
	
}
